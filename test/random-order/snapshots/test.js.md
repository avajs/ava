# Snapshot report for `test/random-order/test.js`

The actual snapshot is saved in `test.js.snap`.

Generated by [AVA](https://avajs.dev).

## bails when --seed is not an integer

> fails with message

    'The --seed flag must be provided with an integer between 0 and 4294967295.'

## bails when --seed is negative

> fails with message

    'The --seed flag must be provided with an integer between 0 and 4294967295.'

## bails when --seed is combined with --no-randomize

> fails with message

    'The --seed flag must not be combined with --no-randomize.'

## bails when randomizing while sortTestFiles is configured

> fails with message

    'sortTestFiles’ must not be configured when randomizing the test file order.'
